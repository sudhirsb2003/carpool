<style>
 .activity{margin-left:90px;padding-bottom:-10px;border-left:0px solid black;padding:5px;}
hr {
  border: 0;
  width: 100%;
  color: silver;
  background-color: silver;
  height: 1px;
}
</style>
  <% @latest_addition.each do |l| %>
      <%= image_tag l.photo.url(:icon) %>
      <span class="activity"> Welcome <%= l.fisrt_name %> ! Joined <%= time_tag l.created_at %>
       has a <%= l.vehicle_type %> and he can accomodate <%= l.accomodation_for %> people .</span><hr>
  <% end %>
</div>
